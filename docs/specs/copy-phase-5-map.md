# Phase 5 Copy Map

This phase is the final tone and de-duplication pass.

## Goals

- Remove repeated lines and repeated ideas across pages.
- Normalize wording for the Historic Gallery.
- Tighten generic labels and card copy on the home page.
- Clean up factual/style consistency such as `LeCount` capitalization.

## Punch List

- Home:
  - Replace `Modern Photos` with `Photos`.
  - Replace the generic contact card heading.
- Cottages index:
  - Remove repeated `personality and history` line already used on the Photos page.
- Historic Gallery:
  - Use `Historic Gallery` consistently in user-facing labels.
  - Mention all gallery groups in the intro.
- Directions:
  - Normalize `LeCount` capitalization.
- Contact:
  - Smooth `availabilities` to `availability` while keeping the historic structure.
